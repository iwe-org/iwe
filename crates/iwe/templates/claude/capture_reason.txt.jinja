IWE memory sweep: call the Agent tool now with subagent_type "{{ agent }}", run_in_background true, and the single-line prompt "Work the capture jobs waiting in this workspace.", then stop immediately without any other output or commentary.